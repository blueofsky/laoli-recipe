# laoli-shorts 变更日志

| 版本 | 变更 |
|------|------|
| 2.15.0 | 新增进度管理（Pipeline Hooks机制 + 制作进度.md）；第8步重命名为视频剪辑（8b去水印改为按需执行）；第9步重命名为提取台词；第10步重写为选取配乐（10a关键词搜索/10b人工生成/10c自动生成三级策略），移除重点音效字段；移除冗余的封面图/封面标题/发布文案占位字段；第11/12步修正输入来源；进度模板补全12步完整列表 |
| 2.14.0 | 管线重构为12步：第8步改为视频预处理（8a变速对齐+8b去水印，输出 `视频处理报告.md`）；新增第10步剪辑指南（`剪辑指南.md`，汇总BGM/音效/封面/标题/文案）；第9步重命名为字幕文稿；封面图移至第11步，发布文稿移至第12步；项目目录新增 `素材/视频/sync/`、`sync_crop/`、`文案/视频处理报告.md`、`文案/剪辑指南.md` |
| 2.12.1 | 移除 `default_projects_dir`：项目目录固定为 `<workspace>/项目`，不再需要配置；同步更新 SKILL.md（删除所有 `default_projects_dir` 引用，项目目录说明改为固定路径）、`preferences-schema.md`（删除 `default_projects_dir` 字段及示例）、`first-time-setup.md`（删除 Step 2，Step 3 重编号为 Step 2）；版本号升级 |
| 2.12.0 | 架构重构：`backends/` 目录内容合并进 SKILL.md（3b节图片生成指令+3c节视频运动指令+第7步TTS参数表），删除 `backends/` 目录；删除 EXTEND.md 中 `default_image_prompt`/`default_video_prompt`/`default_tts_backend` 三个字段（图片/视频提示词通用原则已合并进 SKILL.md，TTS参数直接写在第7步）；`preferences-schema.md` 和 `first-time-setup.md` 同步更新（删除 Step 3/4/5 及对应示例字段）；第4/5/6步调用方式改为直接声明底层 skill 名 |
| 2.11.2 | `seedance-1.5-pro.md` 重命名为 `seedance.md`（文件名不绑版本号）；SKILL.md 第4/5/6步改为读配置文件「调用方式」章节（与第7步架构一致）；修复第2/3步产出格式路径占位符（`项目/` → `<projects_dir>/`） |
| 2.11.1 | 消除跨文件字段名互引：删除 `gpt-image-2.md` 中 `## 风格后缀` 章节（原显式引用方案配置字段名）和冗余的 `## 提示词模板`+`## 示例` 章节（示例含跨文件引用"第3步"）；SKILL.md 和 gpt-image-2.md 中所有 `[风格后缀 from 方案配置]` 改为 `[风格后缀]`；SKILL.md 中"完整风格后缀"统一改为"风格后缀"；方案配置文件章节标题 `### 完整风格后缀` → `### 风格后缀`，`## 风格设定` → `## 叙事设定`（与"视觉风格设定"区分）；方案配置文件重命名 `yanmao.md` → `history-oil.md`（用内容特征命名，不绑频道品牌）；所有引用同步更新 |
| 2.11.0 | 全面去硬编码：视觉风格（风格标签表+风格后缀+定妆照7条规范）迁移到方案配置文件「视觉风格设定」章节；制作规范（视频时长/分镜段数/台词字数/角色数上限）迁移到方案配置文件「制作规范」章节；定妆照模板中风格后缀改为 `[风格后缀 from 方案配置]` 占位符；TTS 工具名从 SKILL.md 移入 `speech-2.8-hd.md`「调用方式」章节；`gpt-image-2.md` 去除硬编码风格后缀，改为引用方案配置；SKILL.md 中所有硬编码值替换为动态读取方案配置 |
| 2.10.0 | 目录重命名 `references/prompts/` → `references/backends/`（适配未来非模型生成后端）；步骤7 TTS 参数去硬编码，移入 `references/backends/speech-2.8-hd.md`；EXTEND.md 新增 `default_tts_backend` 字段；首次设置流程新增 Step 5（TTS 后端选择）；所有 `prompts/` 路径引用替换为 `backends/` |
| 2.9.0 | 新增 EXTEND.md 偏好机制（`references/config/`）；Step 0 阻断加载：首次使用引导设置，后续自动读取偏好；支持项目级/用户级两级配置；频道配置、项目目录、prompt 文件路径全部从 EXTEND.md 动态读取 |
| 2.8.0 | 频道配置抽离到 `references/profiles/`（支持多方案配置共存）；SKILL.md 删除硬编码的频道设定和风格设定；修改为通用管线 skill，替换配置文件即可适配不同方案；第1步输出模板中的"五条风格铁律"改为从配置文件读取 |
| 2.7.0 | 管线铺平：4步→8步（去除嵌套4a/4b/4c，全部顶级步骤）；定妆照提示词合并入角色卡字段（去除独立2b）；删除回填机制；图片质检合并入第5步；新增全局"每步完成后等待确认"规则；新增项目目录规范；第1+2+3步合并保存到 `文案/制作记录.md`；第8步保存到 `文案/后期规格.md` |
| 2.6.0 | 架构重构：移除所有模型特定引用，改为不绑定具体模型；提示词要求提取到 `references/backends/` 目录；修正 seedance-1.5-pro 描述（必须用图生视频i2v） |
| 2.5.0 | 后期规格合并入分镜脚本（输出模板最后一节）；管线5步→4步 |
| 2.4.0 | 定妆照提示词编写移入第2步2b，定妆照生成移入第4步4a；管线6步→5步 |
| 2.3.0 | 新增第4步角色定妆照生成；管线5步→6步 |
| 2.2.0 | 新增第5c步配音生成，逐段调用TTS API |
| 2.1.2 | 角色卡新增"参考图"字段 |
| 2.1.1 | 精简第4步，改为声明式调用 |
| 2.1.0 | 新增核心角色设定+通用视觉准则；风格标签升级为"写实油画风+伦勃朗光影" |
| 2.0.0 | 切换模型：gpt-image-2 + seedance-1.5-pro，去除审核适配 |
| 1.0.0 | 初版（模型：混元3.0 + 内置图生视频） |
