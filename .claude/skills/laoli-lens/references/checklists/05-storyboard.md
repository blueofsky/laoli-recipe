# 第5步-生成分镜图 评审检查清单

## 默认级别：🟡 L2（标准评审）

- [ ] L1 全部检查项（文件存在、数量正确、大小合理）
- [ ] 完整性校验：文件正常生成（非0KB）
- [ ] **图片内容分析**（使用 `laoli vision`）：
  - 逐个分镜分析：`laoli vision --input <图片路径> --prompt "描述图片内容，检查是否与分镜Prompt匹配，角色外貌是否与定妆照一致"`
  - 检查项：内容与Prompt匹配、角色外貌一致、构图合理、无明显AI缺陷
- [ ] 输出路径正确（素材/图片/）
